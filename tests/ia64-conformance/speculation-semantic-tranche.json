{
  "$comment": "SPDX-License-Identifier: GPL-2.0-or-later",
  "schema": "vibtanium.ia64.speculation-semantic-tranche",
  "schema_version": 1,
  "profile": "vibtanium-strict-up",
  "test_id": 706,
  "cases": [
    {
      "id": "ALT-001-INTEGER-NATPAGE-DEFERRAL-EXACT",
      "case_id": 1,
      "normative_token": 95,
      "normative_row": "ALT-001-INTEGER-NATPAGE-DEFERRAL",
      "implementation_rows": ["cpu.opcode.ia64_op_chk_a", "cpu.opcode.ia64_op_chk_s", "cpu.opcode.ia64_op_ld1s", "cpu.opcode.ia64_op_ld1sa", "cpu.opcode.ia64_op_ld2s", "cpu.opcode.ia64_op_ld2sa", "cpu.opcode.ia64_op_ld4s", "cpu.opcode.ia64_op_ld4sa", "cpu.opcode.ia64_op_ld8s", "cpu.opcode.ia64_op_ld8sa"],
      "citation": {"document_id": "intel-ia64-sdm-v3-r2.3", "section": "2: ld and chk", "printed_pages": ["3:34", "3:35", "3:150", "3:151"], "anchor": "Certain exceptions may be deferred rather than generating a fault"},
      "preconditions": ["CPL0 executes with PSR.dt and PSR.ic enabled", "A DTC entry maps an aligned WB page whose PTE memory attribute is NaTPage", "The .s and .sa destinations contain distinct literal payloads with clear NaTs", "Each load uses a distinct base and a positive immediate update equal to one or two datum widths"],
      "inputs": ["Integer widths 1, 2, 4, and 8 bytes", "One ldN.s and one ldN.sa per width", "One integer chk.s recovery branch for the .s destination", "One integer chk.a recovery branch for the .sa destination"],
      "expected_results": ["Each NaTPage reference is deferred without a delivered interruption", "Each destination acquires NaT; its 64-bit data portion is implementation-specific", "Both immediate base updates retire exactly once despite deferral", "CHK.S branches because the .s destination is NaT", "CHK.A branches because deferred .sa allocated no ALAT entry"],
      "allowed_results": ["For every width, any two destination data portions plus the exact two updated bases, two NaTs, and two recovery markers are observed with no pending interruption"],
      "permitted_state_changes": ["The implementation-specific data portions and NaT bits of both destinations", "The two declared base registers", "The two recovery marker registers", "Internal translation and ALAT lookup state required by the instructions"],
      "required_unchanged_state": ["The NaTPage backing memory", "Unrelated GR, NaT, predicate, and memory state"],
      "expected_faults": ["No delivered fault; Data NaT Page is deferred by both control-speculative load types"],
      "fault_priority": ["Enabled predicate and legal operands", "NaTPage translation classification", "Control-speculation deferral", "Base-update retirement", "CHK.S or CHK.A recovery branch"],
      "committed_prefix": "Each speculative load commits only its architecturally required destination NaT and base update; no memory value or ALAT entry is committed for the deferred reference",
      "restart_action": "CHK.S and CHK.A each branch to test-owned recovery code; no interruption retry is involved",
      "applicable_variants": ["ld1.s", "ld1.sa", "ld2.s", "ld2.sa", "ld4.s", "ld4.sa", "ld8.s", "ld8.sa", "integer chk.s", "integer chk.a", "immediate base update"],
      "execution": {"lane": "bare-metal-batch", "probes": ["integer_speculative_nat_page_defer_case"], "result_observations": ["destination payloads and NaTs", "updated bases", "CHK.S recovery marker", "CHK.A recovery marker", "interruption state", "direct typed traces for all loads and checks"]},
      "oracle": {"independence": "independent", "derivation": "All widths, addresses, update values, NaT masks, recovery targets, and terminal markers are test-owned literals or direct arithmetic derived from the cited load/check rules; destination data portions are deliberately not compared because Intel defines them as implementation-specific after deferral, and no production translation, ALAT, or exception helper supplies expected values"},
      "limitations": ["This atomic row covers only integer NaTPage deferral at CPL0; other deferrable and non-deferrable fault classes, successful values, FP forms, false predicates, ALAT-hit optionality, and migration remain separate ALT/MEM/MMU rows"]
    },
    {
      "id": "ALT-002-INTEGER-NO-RECOVERY-FAULTS-EXACT",
      "case_id": 2,
      "normative_token": 96,
      "normative_row": "ALT-002-INTEGER-NO-RECOVERY-FAULTS",
      "implementation_rows": ["cpu.opcode.ia64_op_ld1s", "cpu.opcode.ia64_op_ld1sa", "cpu.opcode.ia64_op_ld2s", "cpu.opcode.ia64_op_ld2sa", "cpu.opcode.ia64_op_ld4s", "cpu.opcode.ia64_op_ld4sa", "cpu.opcode.ia64_op_ld8s", "cpu.opcode.ia64_op_ld8sa"],
      "citation": {"document_id": "intel-ia64-sdm-v2-r2.3", "section": "5.5.5", "printed_pages": ["2:105", "2:106", "2:107"], "anchor": "Qualified Exception Deferral"},
      "preconditions": ["PSR.ic is enabled and serialized", "PSR.it is clear, selecting the no-recovery model regardless of ITLB.ed", "PSR.ed is clear and no DCR-based deferral is active", "Translated-miss probes enable PSR.dt without installing a matching DTC entry", "Alignment probes enable PSR.ac and use either mapped physical RAM or a translated NaTPage whose higher-priority condition always defers"],
      "inputs": ["One ldN.s and one ldN.sa translated-miss probe for widths 1, 2, 4, and 8", "One physical and one concurrent-NaTPage ldN.s/ldN.sa misalignment probe for widths 2, 4, and 8", "Each destination contains a distinct clear-NaT literal", "Each base uses an immediate update equal to its datum width"],
      "expected_results": ["Each qualified Alternate Data TLB condition raises immediately in the no-recovery model", "Each qualified Unaligned Data Reference condition raises immediately in the no-recovery model", "A higher-priority but always-deferred Data NaT Page condition does not hide the lower non-deferred Unaligned condition", "ISR.r and ISR.sp are set while ISR.ed and ISR.ni remain clear", "CR.IIP, CR.IFA, the exception record, and the published M slot identify the exact faulting load", "No destination, destination NaT, base update, or later instruction effect retires"],
      "allowed_results": ["Exactly the declared Alternate Data TLB or Unaligned Data Reference interruption and unchanged declared destination/base state"],
      "permitted_state_changes": ["Architected interruption-collection state", "Internal failed translation lookup state"],
      "required_unchanged_state": ["The destination data and NaT", "The update base", "The backing RAM used by alignment probes", "Unrelated GR, NaT, predicate, ALAT, and memory state"],
      "expected_faults": ["Alternate Data TLB at vector 0x1000 for every translated-miss probe", "Unaligned Data Reference at vector 0x5a00 for every alignment probe"],
      "fault_priority": ["Enabled predicate and legal operands", "Qualified translation conditions", "Deferral removes a qualified NaTPage condition from prioritization", "Qualified non-deferred alignment remains and raises", "Precise interruption collection before any load or update effect"],
      "committed_prefix": "Only instructions before the faulting load commit; the faulting load commits no destination, NaT, update, ALAT, or memory effect",
      "restart_action": "CR.IIP and IPSR.ri identify the faulting M slot for handler retry after software resolves the condition",
      "applicable_variants": ["ld1.s", "ld1.sa", "ld2.s", "ld2.sa", "ld4.s", "ld4.sa", "ld8.s", "ld8.sa", "immediate base update", "no-recovery model", "Alternate Data TLB", "Unaligned Data Reference"],
      "execution": {"lane": "bare-metal-batch", "probes": ["integer_speculative_translation_miss_case", "integer_speculative_unaligned_case"], "result_observations": ["exception kind, vector, source, and address", "CR.IIP, CR.IFA, CR.ISR, and CR.IPSR", "exact fault-slot publication", "destination data and NaT", "unchanged update base", "direct typed trace for every faulting load"]},
      "oracle": {"independence": "independent", "derivation": "The no-recovery control state, width matrix, unmapped virtual address, misaligned physical and NaTPage addresses, initial registers, immediate updates, vectors, ISR bits, and non-retirement rules are direct test-owned literals or arithmetic derived from Intel SDM Volume 2 Section 5.5.5 and the interruption definitions; no production translation or speculative-load helper supplies expected values"},
      "limitations": ["This atomic row closes translated misses and PSR.ac alignment conditions, including concurrent always-deferred NaTPage priority, only; page-not-present, key, access-right, access-bit, data-debug, recovery-model, always-defer, FP, other simultaneous-condition, retry, and migration matrices remain separate ALT/MEM/MMU work"]
    },
    {
      "id": "ALT-002-INTEGER-NO-RECOVERY-PROTECTION-EXACT",
      "case_id": 3,
      "normative_token": 97,
      "normative_row": "ALT-002-INTEGER-NO-RECOVERY-PROTECTION",
      "implementation_rows": ["cpu.opcode.ia64_op_ld1s", "cpu.opcode.ia64_op_ld1sa", "cpu.opcode.ia64_op_ld2s", "cpu.opcode.ia64_op_ld2sa", "cpu.opcode.ia64_op_ld4s", "cpu.opcode.ia64_op_ld4sa", "cpu.opcode.ia64_op_ld8s", "cpu.opcode.ia64_op_ld8sa"],
      "citation": {"document_id": "intel-ia64-sdm-v2-r2.3", "section": "4.1.1 and 5.5.5", "printed_pages": ["2:54", "2:56", "2:105", "2:106", "2:107", "2:152"], "anchor": "Qualified Exception Deferral"},
      "preconditions": ["PSR.ic is enabled and serialized while PSR.it and PSR.ed are clear", "Each translated protection probe installs one deliberately failing DTC entry", "Key probes enable PSR.pk and install explicit valid or read-disabled PKRs", "Access-rights probes execute the load at CPL3 against a PL0 page", "Data Debug probes enable PSR.db and install an exact PL0 read DBR pair", "Data Debug is exercised alone, concurrent with non-deferred Unaligned, and below an always-deferred NaTPage condition"],
      "inputs": ["Page Not Present, Data Key Miss, Key Permission, Data Access Rights, and Data Access Bit for every ld1/2/4/8.s and ld1/2/4/8.sa form", "Aligned Data Debug for every width and class", "Unaligned-plus-Data-Debug for widths 2, 4, and 8 in both classes", "NaTPage-plus-Data-Debug for every width and class", "Each destination contains a distinct clear-NaT literal and each base has a width-sized immediate update"],
      "expected_results": ["All 40 translated protection conditions raise their declared fault immediately", "All 22 Data Debug conditions raise Data Debug immediately", "Data Debug outranks a concurrent non-deferred Unaligned condition", "A higher-priority but deferred NaTPage condition does not hide a lower non-deferred Data Debug condition", "ISR.r and ISR.sp are set while ISR.ed and ISR.ni remain clear", "CR.IIP, CR.IFA, CR.IPSR, the exception record, and the published M slot identify the exact faulting load and active controls", "No destination value, destination NaT, immediate base update, or later instruction effect retires"],
      "allowed_results": ["Exactly the declared Page Not Present, Data Key Miss, Key Permission, Data Access Rights, Data Access Bit, or Data Debug interruption with unchanged destination and base state"],
      "permitted_state_changes": ["Architected interruption-collection state", "Internal failed translation, protection-key, and breakpoint lookup state"],
      "required_unchanged_state": ["The destination data and NaT", "The update base", "Unrelated GR, NaT, predicate, and memory state"],
      "expected_faults": ["Page Not Present at vector 0x5000", "Data Key Miss at vector 0x1c00", "Key Permission at vector 0x5100", "Data Access Rights at vector 0x5300", "Data Access Bit at vector 0x2800", "Data Debug at vector 0x5900"],
      "fault_priority": ["Enabled predicate and legal operands", "Translation and protection qualification", "No-recovery deferral decision", "Data Debug before Unaligned when both remain non-deferred", "Deferred NaTPage removed before lower Data Debug prioritization", "Precise interruption collection before any load or update effect"],
      "committed_prefix": "Only instructions before the faulting load commit; the faulting load commits no destination, NaT, update, ALAT, or memory effect",
      "restart_action": "CR.IIP and IPSR.ri identify the faulting M slot for handler repair and retry",
      "applicable_variants": ["ld1.s", "ld1.sa", "ld2.s", "ld2.sa", "ld4.s", "ld4.sa", "ld8.s", "ld8.sa", "immediate base update", "no-recovery model", "Page Not Present", "Data Key Miss", "Key Permission", "Data Access Rights", "Data Access Bit", "Data Debug"],
      "execution": {"lane": "bare-metal-batch", "probes": ["integer_speculative_protection_fault_case", "integer_speculative_data_debug_case"], "result_observations": ["exception kind, vector, source, and address", "CR.IIP, CR.IFA, CR.ISR, and CR.IPSR", "exact fault-slot publication", "destination data and NaT", "unchanged update base", "direct typed trace for every faulting load"]},
      "oracle": {"independence": "independent", "derivation": "Every PTE, ITIR key, PKR permission bit, CPL, DBR pair, address, width, initial register, update, vector, ISR bit, and non-retirement result is a test-owned literal or direct arithmetic derived from Intel SDM Volume 2 page-access, debug, and qualified-deferral rules; no production translation, key, debug, or speculative-load helper supplies expected values"},
      "limitations": ["This row closes the five selected DCR-controlled protection classes and Data Debug in the no-recovery model only; VHPT Data, Data TLB, recovery-model DCR deferral, always-defer, PSR.ed, FP, other simultaneous conditions, handler retry, and migration remain separate ALT/MMU work"]
    }
  ]
}
